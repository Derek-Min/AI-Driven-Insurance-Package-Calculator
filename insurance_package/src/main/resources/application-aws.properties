# ===============================
# AWS Configuration
# ===============================
aws.access-key = AKIAX6FLU4WA4R4SHJOV
aws.secret-key = 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
aws.region=us-east-1
aws.ses.sender-email=quotation@trust-insurancexyz.xyz

# ===============================
# Spring Mail for AWS SES
# ===============================
spring.mail.host=email-smtp.${aws.region}.amazonaws.com
spring.mail.port=587
spring.mail.username=${AWS_SES_SMTP_USERNAME:}
spring.mail.password=${AWS_SES_SMTP_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# ===============================
# PDF Configuration
# ===============================
quotation.pdf.output-dir=/tmp/insurance-pdfs
company.logo.base64=${LOGO_BASE64:}

# ===============================
# CORS Configuration (Production)
# ===============================
cors.allowed-origins=https://trust-insurancexyz.xyz
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true